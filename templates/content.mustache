<div class="row mt-2">
    <div class="col">
        <div class="card">
            <h3 class="card-header">
                <a href="add_content.php?id={{bot_id}}" class="btn btn-outline-primary float-right">
                {{#str}}add_content, local_cria{{/str}}
                </a>
                <a href="bot_config.php" class="btn btn-outline-primary float-right mr-1">
                    {{#str}}bots, local_cria{{/str}}
                </a>

            </h3>
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>{{#str}}name, local_cria{{/str}}</th>
                            <th>{{#str}}actions, local_cria{{/str}}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#files}}
                        <tr>
                            <td>{{name}}</td>
                            <td>
                                <a href="edit_content.php?id={{id}}" class="btn btn-outline-primary">
                                    {{#str}}edit, local_cria{{/str}}
                                </a>
                                <button class="btn btn-outline-danger delete-content" data-id="{{id}}">
                                    {{#str}}delete, local_cria{{/str}}
                                </button>
                            </td>
                        </tr>
                        {{/files}}
                    </tbody>
                    </table>
            </div>
        </div>
    </div>
</div>